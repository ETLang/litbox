using UnityEngine;

class DebugSniffer : MonoBehaviour
{
    void Update()
    {
        // var obj = GetComponent<RTSpotLight>();

        // if (Input.GetKeyDown(KeyCode.D))
        // {
        //     //Debug.Log($"DebugSniffer: {obj.intensity} intensity, {obj.bounces} bounces");
        // }
    }
}